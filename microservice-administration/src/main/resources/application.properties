spring.application.name=microservice-admin
server.port=${SERVER_PORT:9008}
spring.config.import=configserver:${SPRING_CLOUD_CONFIG_URI:http://localhost:9101}
spring.profiles.active=${SPRING_PROFILES_ACTIVE:dev}
app.licence.rsaPrivateKey=${APP_LICENCE_RSA_PRIVATE_KEY}
#app.licence.rsa-public-key=${APP_LICENCE_RSA_PUBLIC_KEY}
