# Configuration API Gateway pour Docker

# Tracing Sleuth 
spring.cloud.compatibility-verifier.enabled=false

# Configuration Eureka pour Docker
#eureka.client.serviceUrl.defaultZone=http://eureka-server:8761/eureka/
eureka.client.serviceUrl.defaultZone=http://127.0.0.1:8761/eureka/
eureka.client.fetch-registry=true
eureka.client.register-with-eureka=true

# Préférer l'IP dans Eureka (important pour Docker)
eureka.instance.prefer-ip-address=true
eureka.instance.hostname=gateway

# Health check pour Eureka
eureka.instance.health-check-url-path=/actuator/health
eureka.instance.status-page-url-path=/actuator/info

# Logging (décommenter si besoin de debug)
#logging.level.org.springframework.cloud.gateway=DEBUG
#logging.level.reactor.netty=DEBUG