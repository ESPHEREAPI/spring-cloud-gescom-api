# Configuration du Config Server pour Docker
server.port=9101

# Configuration du repository Git LOCAL
spring.cloud.config.server.git.uri=file://${HOME}/cloud-config-gescom
spring.cloud.config.server.git.default-label=main

# Désactiver la recherche de repository natif
spring.cloud.config.server.native.search-locations=classpath:/config

# Activer le profil git
spring.profiles.active=git

# Configuration de sécurité (optionnel, à activer si besoin)
#spring.security.user.name=admin
#spring.security.user.password=admin123

# Logging pour debug
logging.level.org.springframework.cloud.config=DEBUG
logging.level.org.springframework.boot=INFO

# Désactiver Eureka pour le config-server (il démarre en premier)
spring.cloud.config.discovery.enabled=false
eureka.client.enabled=false